---
name: test-plan
description: 'Moved — install the tw-qa plugin and use /qa. This location is kept for backwards compatibility only.'
version: 0.8.0
user-invocable: false
---

# This skill has moved

The `test-plan` skill was renamed to `qa` and now lives in the separate **tw-qa** plugin.

Install it and use `/qa`:

```bash
claude plugin install tw-qa@techwave     # Claude Code
copilot plugin install tw-qa@techwave    # Copilot CLI
```
